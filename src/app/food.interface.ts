export interface Food {
    name: string;
    // Potential future properties
    // description?: string;
    category?: string;
    // nutritionalInfo?: NutritionalInfo; // Another interface could be defined for this
}
  